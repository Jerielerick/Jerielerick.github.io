<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>prueba de rol es ok</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 20px;
            padding: 20px;
            border: 1px solid #3498db; /* Azul */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #ecf0f1; /* Gris claro */
        }

        h1, h2, h6 {
            border-bottom: 1px solid #3498db; /* Azul */
            padding-bottom: 5px;
            margin-bottom: 10px;
        }

        h1 {
            color: #2c3e50; /* Gris oscuro */
        }

        h2 {
            color: #3498db; /* Azul */
        }

        h6 {
            color: #7f8c8d; /* Gris */
        }
    </style>
</head>
<body>
    <div>
        <h1>Pronostico registrado</h1>
        <div>
            <h2>pronostico1: <%= pronostico1 %></h2>
            <h2>pronostico2: <%= pronostico2 %></h2>
            <h2>pronostico3: <%= pronostico3 %></h2>
            <h2>pronostico4: <%= pronostico4 %></h2>
            <h2>pronostico5: <%= pronostico5 %></h2>

    </div>

    <div>
        <h6>*Bienvenido, ahora estás registrado en nuestra página</h6>
    </div>

    <!-- Agrega el botón con el enlace a la ruta deseada -->
    <a href="/index.html" style="display: block; margin-top: 10px; padding: 10px; background-color: #3498db; color: #fff; text-decoration: none; border-radius: 5px; text-align: center;">Volver al inicio</a>
</body>
</html>
